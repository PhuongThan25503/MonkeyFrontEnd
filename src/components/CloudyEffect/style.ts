import { StyleSheet } from "react-native";
export const CloudStyle = StyleSheet.create({
  CanvasContainer: {
    position: 'absolute',
    width: '100%',
    height: '100%',
  },
  ViewBound: {
    width: '100%',
    flexDirection: 'row',
    flexWrap: 'wrap',
    alignContent: 'center',
    borderWidth: 2,
    borderColor: 'yellow'
  }
});
