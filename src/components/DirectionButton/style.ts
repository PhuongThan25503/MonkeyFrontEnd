import { StyleSheet } from "react-native";

export const DirectButtonStyle = StyleSheet.create({
  box:{
    width: '50%',
    height: '80%',
    alignContent: 'center',
    alignItems:'center'
  }
});
